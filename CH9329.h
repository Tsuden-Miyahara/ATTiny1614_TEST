/*
 * CH9329.h
 */
#include <Arduino.h>

//#include <SoftwareSerial.h>
/*
class Keyboard {
  public:
    Keyboard(Serial *ser);
    int SendPacket(char *buf, char blen);
    void Press(byte ckey, byte ukey);
  protected:
    Serial *_ser;
};
*/